import heroImg from "/public/home.jpg";
import Hero from "@/components/Hero";

const HomePage = () => {
  return <Hero title={"Professional Cloud Hosting"} image={heroImg} />;
};

export default HomePage;
